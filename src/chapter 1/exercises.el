;; Generate an error message by evaluating an appropriate symbol that is not within parantheses
generate-error-message

;; Generate an error message by evaluating an appropriate symbol that is between parantheses
(generate-error-message)

;; Create a counter that increments by two
(setq counter 0)

(setq counter (+ counter 2))

counter

;; Write an expression that prints a message to the echo area when evaluated
(message "This message is printed to the echo area")



